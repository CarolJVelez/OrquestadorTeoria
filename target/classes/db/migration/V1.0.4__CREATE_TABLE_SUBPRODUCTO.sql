CREATE TABLE MA_SUBPRODUCTO
(
   SUBPROCUTO_ID    SERIAL primary key  NOT NULL,
   PRODUCTO_ID      INTEGER             NOT NULL,
   NOMBRE           VARCHAR(30)         NOT NULL,
   CODIGO           VARCHAR(3)          NOT NULL,
   ACTIVO           BOOLEAN             NOT NULL
 );

ALTER TABLE MA_SUBPRODUCTO
ADD CONSTRAINT FK_MA_PRODUCTO FOREIGN KEY (PRODUCTO_ID) REFERENCES DIGITALSF.MA_PRODUCTO (PRODUCTO_ID);